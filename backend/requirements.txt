# ─── Core Framework ────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
uvloop==0.19.0
httptools==0.6.1
python-multipart==0.0.9
websockets==12.0

# ─── Database ──────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# ─── Cache / Queue ─────────────────────────────────────────────────────────
redis==5.0.4
celery[redis]==5.4.0
kombu==5.3.7

# ─── AI / Agents ───────────────────────────────────────────────────────────
anthropic==0.28.0
langchain==0.2.5
langchain-anthropic==0.1.15
langchain-community==0.2.5
langgraph==0.1.14
langsmith==0.1.77
llama-index==0.10.47
llama-index-llms-anthropic==0.1.13
tavily-python==0.3.3

# ─── Vector DB ─────────────────────────────────────────────────────────────
qdrant-client==1.9.1

# ─── Document Processing — ALL INTEGRATED ──────────────────────────────────
# PyMuPDF: PDF text + image extraction
pymupdf==1.24.5
# python-docx: DOCX text + table extraction
python-docx==1.1.2
# Tesseract OCR wrapper
pytesseract==0.3.10
Pillow==10.3.0
# OpenCV: image preprocessing, deskew, edge detection
opencv-python-headless==4.9.0.80
# Camelot: PDF table extraction (lattice + stream)
camelot-py[cv]==0.11.0
# Tabula: PDF table extraction (Java-based)
tabula-py==2.9.3
# Unstructured.io: universal document partitioning
unstructured[pdf,docx,pptx,xlsx,image]==0.14.6
# Apache Tika: metadata + content extraction (Java server)
tika==2.6.0
# Pandoc Python wrapper
pypandoc==1.13

# ─── HTTP / APIs ───────────────────────────────────────────────────────────
httpx==0.27.0
aiohttp==3.9.5
requests==2.32.2

# ─── File Storage (S3 / MinIO) ─────────────────────────────────────────────
boto3==1.34.122
aioboto3==13.0.0
python-magic==0.4.27

# ─── Authentication ────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
jwcrypto==1.5.6

# ─── Validation / Schemas ──────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.1

# ─── NLP / Text Processing ─────────────────────────────────────────────────
nltk==3.8.1
spacy==3.7.4
scikit-learn==1.5.0
numpy==1.26.4
scipy==1.13.0
# textstat: readability metrics (Flesch, Gunning Fog, etc.)
textstat==0.7.3
langdetect==1.0.9
# sentence-transformers: semantic embeddings for similarity
sentence-transformers==3.0.1

# ─── Reference / Citation (FREE APIs) ─────────────────────────────────────
# habanero: CrossRef API client
habanero==1.2.3
# scholarly: Google Scholar (no API key required)
scholarly==1.7.11
# arxiv: arXiv API client
arxiv==2.1.0
# biopython: PubMed / NCBI E-utilities
biopython==1.83

# ─── Document Generation ───────────────────────────────────────────────────
reportlab==4.2.0
jinja2==3.1.4
weasyprint==62.3

# ─── Scientific Visualisation (chart rendering → embedded PNG in docx) ─────
matplotlib==3.9.0
seaborn==0.13.2
pandas==2.2.2

# ─── Monitoring / Observability ────────────────────────────────────────────
prometheus-client==0.20.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0

# ─── Utilities ─────────────────────────────────────────────────────────────
python-dotenv==1.0.1
structlog==24.2.0
tenacity==8.3.0
python-slugify==8.0.4
cryptography==42.0.7

# ─── Interactive CLI ────────────────────────────────────────────────────────
rich==13.7.1
questionary==2.0.1
click==8.1.7
