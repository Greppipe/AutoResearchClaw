# ── CLI-mode requirements (no web server / DB / monitoring needed) ───────────
# Install with:  pip install -r requirements.cli.txt

# ─── Core AI / LLM ──────────────────────────────────────────────────────────
anthropic>=0.50.0
langchain>=0.2.0
langchain-anthropic>=0.1.15
langchain-community>=0.2.0
langchain-core>=0.2.0
langgraph>=0.2.0
langsmith>=0.1.0
tavily-python>=0.3.0

# ─── ML / Embeddings ────────────────────────────────────────────────────────
sentence-transformers>=2.7.0
scikit-learn>=1.3.0
numpy>=1.24.0
scipy>=1.10.0

# ─── Document Extraction ────────────────────────────────────────────────────
pymupdf>=1.23.0
python-docx>=1.0.0
pytesseract>=0.3.10
Pillow>=10.0.0
opencv-python-headless>=4.8.0
pandas>=2.0.0
openpyxl>=3.1.0
tabula-py>=2.9.0

# ─── Charts / Visualisation ─────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ─── HTTP / APIs ────────────────────────────────────────────────────────────
httpx>=0.24.0
aiohttp>=3.9.0
requests>=2.30.0

# ─── Storage (import shim — patched at runtime for local mode) ──────────────
aioboto3>=11.0.0
boto3>=1.28.0
qdrant-client>=1.7.0

# ─── NLP / Text ─────────────────────────────────────────────────────────────
nltk>=3.8.0
textstat>=0.7.0
langdetect>=1.0.9

# ─── Reference APIs ─────────────────────────────────────────────────────────
habanero>=1.2.0
scholarly>=1.7.0
arxiv>=2.0.0
biopython>=1.82

# ─── Auth (import shim only) ────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
cryptography>=40.0.0

# ─── Config / Validation ─────────────────────────────────────────────────────
pydantic>=2.5.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# ─── Utilities ──────────────────────────────────────────────────────────────
structlog>=24.0.0
tenacity>=8.0.0
jinja2>=3.1.0

# ─── CLI Display ─────────────────────────────────────────────────────────────
rich>=13.0.0
questionary>=2.0.0
click>=8.1.0

# ─── Free LLM Providers (install the one you need) ──────────────────────────
# Ollama  — local, free
langchain-ollama>=0.1.0
# Groq    — free API tier, very fast
langchain-groq>=0.1.0
# Gemini  — free API tier
langchain-google-genai>=1.0.0

# ─── Optional heavy packages (handled gracefully on failure) ──────────────────
# camelot-py[cv]==0.11.0       → needs Ghostscript
# tika==2.6.0                  → needs Java
# unstructured[pdf,docx,pptx]  → large, complex deps
